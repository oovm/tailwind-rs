[workspace]
members = ["projects/*"]
default-members = [
    "projects/tailwind-to-css",
    "projects/tailwind-cli",
    "projects/tailwind-rs",
]
exclude = [
    "projects/.DS_Store",
    "projects/tailwind-show",
]

[profile.release]
lto = true
panic = "abort"
